#include <iostream>
#include "ppu/mpc/dealer/dealer_tfp.h"

int main(){    
    std::vector<Dealer::LR_set> lr_set;
    lr_set.resize();
    auto dealer = ;
    lr_set[]=dealer->lr("FM128",4,5);
    return 0;
}